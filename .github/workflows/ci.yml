name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    env:
      CI: 'true'
      RELEASE: 'true'
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: Setup tmate session
        uses: mxschmitt/action-tmate@v3
